{
  "name": "carbon-offset-demo",
  "version": "1.0.0",
  "description": "A demonstration for the grid offset capability in Carbon",
  "scripts": {
    "build": "rm -Rf dist/* && cp node_modules/carbon-components/css/carbon-components.css* dist/ && cp index.html dist/",
    "start": "./node_modules/http-server/bin/http-server dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Stephane Rodet",
  "license": "ISC",
  "devDependencies": {
    "carbon-components": "../carbon-components/"
  },
  "dependencies": {
    "http-server": "^0.11.1"
  }
}
